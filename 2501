#include <stdio.h>
int main(void) {
	int n, k, i;
	int j=0;
	scanf("%d %d", &n, &k); 
	for (i = 1; i <= n; i++)
	{
		if (n % i == 0) {
			j++;

			if (j == k)
				break;

		}
		
	}


	if (i <= n)
		printf("%d", i);
	else
		printf("0");
	return 0;
}
